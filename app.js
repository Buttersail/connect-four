// let currentPlayer = 'red'
let gameActive = true

// Seems to be only the first cell that reacts to this, figure out why
document.querySelector('.cell').onclick = function () {
  console.log('test')
}
